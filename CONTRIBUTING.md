# This file is for internal use by the MergeOS bounty pipeline.
# It is not intended to be committed to the repository.

# Setup instructions for the project, including dependencies and build steps.

# Example:
# uv pip install -e ".[dev]"
# uv run pytest

# If you need to make changes to CONTRIBUTING.md, do them here first.
# After testing, commit and push them to the repository.
